Write a function called calculatedPositiveNegativeTotals that performs the following:
1. Accepts an array of integers as input.
2. Returns an object with two properties:
    - positiveTotal: The sum of all posistive numebers
    - negativeTotla: the sum of all negative numebers

Example:
const numbers = [1, -2, 3, -4, 5, -6, 0]